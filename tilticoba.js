$(document).ready(function(){
    $('.cobajs').tilt({
    scale: 1.2,
      glare: true,
      maxGlare: .5,
  })

  $('.sm-tilt').tilt({
    scale: 0.9,
      glare: true,
      maxGlare: .9
  
  })
  
})